{
  "installCommand": "curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --channel 9.0 --install-dir /tmp/dotnet",
  "buildCommand": "export DOTNET_ROOT=/tmp/dotnet && export PATH=/tmp/dotnet:$PATH && dotnet run -- build",
  "outputDirectory": "site",
  "framework": null
}
